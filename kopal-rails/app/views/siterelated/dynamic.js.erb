if(!kopal)
  var kopal = {};

if(!kopal.route)
  kopal.route = {};

kopal.route.root = "<%= @kopal_route.root %>";
kopal.identity = "<%= @profile_user.kopal_identity %>";
kopal.RAILS_ENV = "<%= Rails.env %>";
